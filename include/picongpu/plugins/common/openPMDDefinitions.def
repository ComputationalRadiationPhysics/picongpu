/*
 * SPDX-FileCopyrightText: Franz Poeschel, Rene Widera
 *
 * SPDX-License-Identifier: GPL-3.0-or-later
 */
#pragma once

namespace picongpu::openPMD
{
    namespace PreferredFlushTarget
    {
        inline constexpr char const* const Disk = R"({"adios2": {"engine": {"preferred_flush_target": "disk"}}})";
        inline constexpr char const* const Buffer = R"({"adios2": {"engine": {"preferred_flush_target": "buffer"}}})";
    } // namespace PreferredFlushTarget
} // namespace picongpu::openPMD
