/*
 * SPDX-FileCopyrightText: Axel Huebl, Rene Widera, Marco Garten, Benjamin Worpitz, Richard Pausch
 *
 * SPDX-License-Identifier: GPL-3.0-or-later
 */

#pragma once

#include "picongpu/particles/manipulators/manipulators.def"
#include "picongpu/particles/startPosition/functors.def"

#include <pmacc/math/operation.hpp>

namespace picongpu
{
    namespace particles
    {
        /** a particle with a weighting below MIN_WEIGHTING will not
         *      be created / will be deleted
         *  unit: none
         */
        constexpr float_X MIN_WEIGHTING = 10.0;

        namespace startPosition
        {
            struct RandomParameter2ppc
            {
                /** Count of particles per cell at initial state
                 *  unit: none
                 */
                static constexpr uint32_t numParticlesPerCell = 2u;
            };

            using Random2ppc = RandomImpl<RandomParameter2ppc>;

        } // namespace startPosition
    } // namespace particles
} // namespace picongpu
